$data modify storage te_be:undo selection.$(undo_index).blocks.$(current_block_index).x set $(x)
$data modify storage te_be:undo selection.$(undo_index).blocks.$(current_block_index).x set $(y)
$data modify storage te_be:undo selection.$(undo_index).blocks.$(current_block_index).x set $(z)

#function util